#include "MonsterAgent.h"

MonsterAgent::MonsterAgent(Monster& ref) : monster_ref_(ref) {
}

void MonsterAgent::take_action() {
    /*std::vector<Monster> monsters_in_vision = tower_ref_.radar();

    if (monsters_in_vision.size() != 0)
        tower_ref_.shoot();*/
}
